
# JAVA基础

## 概述

##### 什么是程序？

+ 程序： 计算机执行某些操作或解决某个问题而编写的一系列有序指令的集合  

##### Java的技术平台体系

+ JavaSE、JavaEE和JavaME

##### Java的特点

+ 面向对象

+ 健壮的：强类型机制、异常处理、垃圾自动回收等是Java程序健壮性的保证

+ 跨平台：一个编译好的.class 文件可以在多个系统下运行

+ 解释型语言[了解]

##### Java虚拟机【JVM】

​			JVM 是一个虚拟的计算机， 具有指令集并使用不同的存储区域。 负责执行指令， 管理数据、 内存、 寄存器， 包含在
JDK 中 ；对于不同的平台， 有不同的虚拟机；Java 虚拟机机制屏蔽了底层运行平台的差别， 实现了“一次编译， 到处运行” 。

<img src="docs\Java\source\jvm1.png" alt="image-20220208233133000" style="zoom:50%;" />

##### JDK与JRE

+ JDK (Java Development Kit Java 开发工具包)
  JDK = JRE + Java 的开发工具 [java, javac,javadoc,javap 等]  
+ JRE(Java Runtime Environment Java 运行环境)
  JRE = JVM + Java 的核心类库[类] 

##### Java程序的执行流程

+ .![image-20220209215142477](docs\Java\source\java程序执行流程.png)

##### Java转义字符

+ \t ： 一个制表位， 实现对齐的功能
  \n ： 换行符
  \\ ： 一个\
  \" :一个"
  \' ： 一个'
  \r :一个回车  

Java的注释类型

+ 单行注释 //
+ 多行注释 /* */
+ 文档注释 /** */  

## 变量

##### 概念

+ 变量相当于内存中一个数据存储空间的表示， 可以把变量看做是一个房间的门牌号， 通过门牌号我们可以找到房
  间， 而程序可以通过变量名可以访问到变量(值)
+ 变量 = 数据类型 + 变量名 + 变量值

##### 变量的命名规范(来自运算符那节p110)

##### Java程序中 + 号的使用

+ 当+号左右两边是数值类型时，做加法运算
+ 当+号左右两边存在字符串时，做拼接运算

##### Java的数据类型

+ 结构![image-20220209220603852](docs\Java\source\java数据类型.png)

+ 基本类型

  + 整型![image-20220209220953921](docs\Java\source\基本数据类型之整型.png)

  + 举例

    ```java
    int i = 1;
    byte b = 1;
    short s = 1;
    long l = 1L;
    ```

  + 浮点类型![image-20220209221424611](docs\Java\source\基本数据类型之浮点类型.png)

  + 举例

    ```java
    float f = 3.14f;
    double d = 3.1415; //默认为double
    ```

  + 说明：

    + 浮点数=符号位+指数位+尾数位  
    + 尾数部分可能丢失， 造成精度损失(小数都是近似值）
    + 浮点常量的两种表示类型：
      + 十进制数形式：5.12  512.01f  .521
      + 科学计数法：5.12e2（5.12 * 10的2次方）5.12e-2（5.12 / 10的2次方）

  + 字符类型

    + 字符常量使用单引号' ' 括起来的单个字符

      ```java
      char c = 'A';
      char c1 = '\n'; //允许使用转义字符
      char c2 = 97; //与 char c2 = 'a';等价
      ```

    + char 的本质是一个整数，输出的是unicode对应的字符

    + char可以进行运算，相当于一个整数

    + 字符编码表

      + [ASCII 码](http://www.asciima.com/)  
      + [Unicode 编码](https://home.unicode.org/) 
      + [UTF-8 编码](https://www.zhihu.com/question/23374078/answer/65352538) 

  + 布尔类型：Boolean

    + 占一个字节
    + 只允许取值 true 或者 false

+ 基本数据类型转换

  + 自动类型转换
    + 自动类型转换注意和细节  
  + 强制类型转换
    + 强制类型转换细节说明  

+ 基本数据类型和 String 类型的转换  

  + 注意事项
  + 

## 运算符

##### 算数运算符（写表格）

##### 关系运算符

##### 逻辑运算符

+ 短路与&&，短路或||，取反！
+ 逻辑与&，逻辑或 |，逻辑异或^
+ （截图或表格）

##### 赋值运算符

+ 基本 和 复合 赋值运算符
+ 特点(1234)

##### 三元运算符

+ 基本语法：条件表达式 ？表达式1 ：表达式2；
+ 基本规则：
  + 若 **条件表达式** 为 **true** ，运算后的结果是 **表达式1** 
  + 若 **条件表达式** 为 **false** ，运算后的结果是 **表达式2**

##### 运算符的优先级

+ （表格）

##### 关键字（制表）

+ 用于定义 **数据类型**
+ 用于定义 **数据类型值**
+ 用于定义 **流程控制**
+ 用于定义 **访问修饰权限**
+ 用于定义 **类与类之间的关系**
+ 用于 **建立、引用和判断实例**
+ 用于 **异常处理**
+ 用于 **包**
+ 其他修饰符

##### 保留字

+ 尚未使用，后面的版本也许会用（制表）

## 基本输入输出与进制

##### 键盘上输入语句

```java
//example
```

##### 进制

+ 二进制：0，1 满2进1，以0b或0B开头
+ 十进制：0-9，满10进1
+ 八进制：0-7，满8进1，以数字0开头表示
+ 十六进制：0-9及A(10)-F(15)，满16进1，以0x会0X开头表示，A-F不区分大小写

##### 进制的转换

+ 二转十
+ 八转十
+ 十六转十
+ 十转二
+ 十转八
+ 十转十六
+ 二转八
+ 二转十六
+ 八转二
+ 十六转二

##### 位运算

+ 位运算符
+ 原码、反码、补码

## 程序控制结构

##### 概述

+ 顺序结构：程序从上到下逐行执行，中间没有任何判断和跳转
+ 分支控制
+ 循环控制

##### 分支结构

+ 单分支 if
  + 截图（流程图）
+ 双分支if-else
  + 图
+ 多分支if-else if-...-else
  + 图
+ 嵌套分支（建议不超过3层）
+ switch分支结构
  + 结构
  + 注意事项

控制结构

+ for循环控制

  + 基本语法
  + 流程图
  + 注意事项和细节说明

+ while循环控制

  + 基本语法
  + 流程图
  + 注意事项和细节说明

+ do...while循环控制

  + 基本语法
  + 流程图
  + 注意事项和细节说明

+ 多重循环控制

  + 基本介绍

+ 跳转控制语句-break

  + 基本介绍
  + 基本语法
  + 流程图
  + 注意事项和细节说明

+ 跳转控制语句-continue

  + 基本介绍
  + 基本语法
  + 流程图
  + 注意事项和细节说明

+ 跳转控制语句-return

  + return使用在方法中，表示跳出所在的方法

  + 举例

    ```java
    //example
    ```

    



## 数组、排序和查找

##### 数组

+ 概述

  + 数组就是一组数据
  + 数组可以存放多个同一类型的数据
  + 数组也是一种数据类型，是引用类型

+ 数组的使用

  + 动态初始化

    ```java
    //example
    ```

  + 静态初始化

    ```java
    //example
    ```

  + 数组使用细节

    + 1-7

  + 举例

    ```java
    //exmaple
    ```

  + 数组的赋值机制

    + 基本数据类型赋值
    + 默认是引用传递，赋值为地址
    + 内存图（图）

  + [数组的拷贝](https://blog.csdn.net/weixin_42220532/article/details/84200634)

    + for
    + clone()
    + System.arrayCopy()
    + Arrays.copyof()

  + 数组的反转

    + 举例

  + 数组的扩容

    +  [举例](https://www.cnblogs.com/gw811/archive/2012/10/07/2714252.html)

  ##### 排序

  + 内部排序

    + 指将需要处理的所有数据**都加载**到内存中进行排序
    + 包括：交换排序、选择排序 和 插入排序

  + 外部排序

    + 数据量过大，无法全部加载到内存中，需要借助外部存储进行排序
    + 包括：合并排序 和 直接合并排序

  + 冒泡排序（动图）

    + 举例

      ```java
      //example
      ```

  ##### 查找

  + 顺序查找
  + 二分查找

  ##### 二维数组

  + 举例

  + 动态初始化

    + 语法

      ```java
      //
      ```

    + 二维数组在内存中的存在形式（图）

  + 静态初始化

    + 语法：

      类型 数组名  = {{值1，值2..},{值1，值2..},{值1，值2..}}

      ```java
      //举例
      	@Test
          public void testMatrix(){
              int[][] matrix = {
                      {1, 2, 3},
                      {4, 5, 6}
              };
              for (int[] ints : matrix) {
                  System.out.printf(Arrays.toString(ints));
              }
          }
      
      //输出：[1, 2, 3][4, 5, 6]
      ```

  + 二维数组使用细节和注意事项

    + **一维数组**声明方式
    + **二维数组**声明方式
    + 二维数组本质是由多个一维数组组成

  

## 面向对象

##### 类与对象

+ 类与对象的区别和联系

  + 1
  + 2
  + 3

+ 对象在内存中的存在形式（图）

+ 属性、成员变量、字段

  + 概述：成员变量 = 属性 = field（字段）

  + **属性**是**类**的一个组成部分，一般是**基本**数据类型，也可以是**引用**类型（对象，数组）

  + 举例

    ```java
    public class Person {
        private String name;
        private int age;
        private String[] courses;
    }
    ```

+ 对象的创建

  + 先声明再创建
  + 直接创建

+ 如何访问属性

  + 类属性**非私有**时：对象名.属性名

    ```java
    //
    ```

    

  + 类属性**私有（private）**时：通过get/set方法操作属性

    ```java
    //
    ```

+ 类和对象的内存分配机制

  + java内存的结构分析（图）

    + 栈
    + 堆
    + 方法区

  + java对象的流程分析

    + 先加载类信息（属性、构造方法和方法信息，只执行一次）

    + 在堆中分配空间，进行默认初始化

    + 栈指向堆中的对象地址

    + 进行指定初始化，例如：

      ```java
      p.name = "小羊";
      ```

##### 成员方法

+ 作用：

  + 提高代码的复用性
  + 将逻辑实现的细节封装起来，然后提供给其他用用户调用

+ 基本结构

  ```java
      /*
          访问修饰符 返回数据类型 方法名（形参列表...）{
              ....
              return 返回值；
          }  
      */
      private String showPeople(String name, int age) {
          return "姓名：" + name + "," + "年龄：" + age;
      }
  ```

+ 形参列表

+ 返回数据类型

+ 方法主体

+ return 不是必须

+ 方法的调用机制

  + 同一个类：直接调用
  + 跨类：需要实例化对象通过对象名调用
  + 图
  + 小结（p230）
    1. 当程序
    2. 2
    3. 3
    4. 4
    5. 5

+ 成员方法传参机制

  + **基本数据类型**传参：传递 **值**（值拷贝），形参的任何改变不会影响实参

    ```java
    
    ```

  + **引用类型**传参：传递 **地址**（传递的值是地址），形参可以影响实参

    ```java
    
    ```

+ 方法的递归调用

+ 方法的重载（Overload）

  + 概念：**方法名相同**，但**参数类型**，**个数**，或者**顺序不同**；对 **返回值** 没有要求

  + 作用：减少起方法名与记方法名的麻烦

    ```java
        private String showPeople(String name, int age) {
            return "姓名：" + name + "," + "年龄：" + age;
        }
        
        private void showPeople(int age,String name){ //顺序不同
            System.out.printf(name + ":" + age);
        }
        
        public int showPeople(String name,String gender,int age){ //个数不同
            System.out.println(name + gender);
            return age;
        }
    
        public String showPeople(String name,String age){ //类型不同
            return name + age;
        }
    ```

+ 可变参数

+ 作用域

+ 构造方法 / 构造器

+ 对象创建的流程分析

+ this关键字

##### 包

+ 基本语法

  + package 包名

  ```java
  package com.alibaba;
  ```

+ 作用
  + 区分相同名字的类
  + 类很多时，可以更好的管理类
  + 控制访问范围

##### 访问修饰符

+ 基本介绍
  + 1234
+ 访问范围（制表）

##### 面向对象的三大特征

​	封装、继承和多态

+ 封装

  + 概述

  封装是。。。。

  + 封装的理解和好处
    + 1
    + 2
  + 封装的实现步骤
    + 1
    + 23
    + 3
  + 举例

  ```java
  //example	
  ```

+ 继承

  + 概述和示意图

  继承是，，，（插图）

  + 基本语法

  ```java
  //
  ```

  + 举例

  ```java
  //
  ```

  + 继承的好处
    + 1
    + 2
  + 继承的深入理解
    + 1-10（3画图）
  + 继承的内存分析（图）

  ```java
  // p331 案例一分析
  //执行顺序：
  B的无参构造首先调用有参构造，有参构造再调用默认的super；
  即A的无参构造，再输出
  ```

  + super关键字

    + 基本介绍

    + 基本语法

    + 举例

      ```java
      //
      ```

      

    + 注意事项与使用细节
      + 123
    + 方法的重写（override）和 重载（overload）的区别（制表）

+ 多态

  + 基本介绍

  + 多态的具体实现

    + 方法的多态

      ```java
      //
      ```

    + 对象的多态

      前提：

      本质：父类的引用指向子类对象

      + 多态的向上转型

        + 123

          ```java
          //
          ```

      + 多态的向下转型

        + 1234

          ```java
          //
          ```

##### Java的动态绑定机制

+ 概述

  + 当用户调用方法时：**该方法会和该对象的内存地址/运行类型**绑定
  + 当用户调用属性时：**没有动态绑定机制**，哪里声明，哪里使用

+ 举例

  ```JAVA
  //
  ```

Object类详解

+ equals方法

  + == 与 equals 方法的对比

    + 1-5

    + 源码

      ```java
      //
      ```

  + 重写equals方法

    + 举例

      ```java
      //
      ```

  + hashCode方法

    + API截图
    + 小结
      + 1-6

  + toString方法

    + 概述

    + 重写toString方法

      ```java
      //
      ```

    + 当直接输出一个对象时，会默认调用toString方法

      ```java
      People p = new People();
      sout(p);
      //等价
      sout(p.toString());
      ```

  + finalize方法（很少用）

    使用场景：

    + 123





## 枚举

## 注解

## 异常

## 常用类

## 泛型

## 导图
![导图](docs\Java\source\java_basic.png)
